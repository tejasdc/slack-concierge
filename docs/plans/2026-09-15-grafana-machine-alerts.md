# Grafana machine alerts
## Operating profile and acceptance
Personal single-operator system, seven existing Grafana conditions for Thinkering/AX41. No existing general machine endpoint found; capture routes publish as the user and are unsuitable. Deliver bot-authored firing/recovery with fingerprint and startsAt identity, deduplicate, bound read-only investigations. No email or user captures. Thinkering owns rule/contact activation. No new scheduler, provider daemon restart, rule editing or production synthetic traffic here.

## Complete change
1. Fixed POST /alerts/grafana on existing origin https://95-217-119-40.sslip.io. Existing unprivileged ingress validates dedicated Authorization: Bearer credential and Content-Type: application/json, then forwards normalized allowlisted data via existing capture_queue authenticated loopback control listener. No Slack credential at public edge; no capture rows. Streaming body limit256KiB, max64 instances. Envelope/malformed identity errors reject atomically. Unknown conditions are skipped with an allowlisted diagnostic and omitted-count in the known alerts; if none are known return422. Grafana truncation is visible on the alert and receipt, not a false complete notification. Configure Max Alerts=64; current live rules have seven instances total. Forward failure/timeouts return503 without acknowledgement; Grafana owns retry/re-notification. There is no edge queue. A host-wide outage also makes this receiver unavailable until the host recovers; Grafana still evaluates and records delivery failures externally.
2. Derive route bearer using HMAC-SHA256(existing capture_queue key, versioned Grafana domain string). Never disclose queue credential. Existing systemd credential already reaches ingress; no unit change. Tracked CLI exclusively creates or verifies /etc/concierge/grafana-alerts.token mode0600. Operator loads this private file into Grafana's secure Bearer credentials field, never URL/query or chat.
3. Source externalURL restricted to https://gracefulfennel1915.grafana.net; exact labels.alertname allowlist: ThinkeringExternalUnavailable, ThinkeringBackupStale, AX41ResourcePressure, ThinkeringDurableBacklogStale, AX41CollectionUnavailable, AX41LogCollectionStalled, PersonalTelemetryAllowance. These names were verified in the live Grafana alerts API (seven instances) and remote-box/observability/alerts.json. Server owns safe summary and agent prompt; arbitrary labels/annotations are discarded. Persist fingerprint, normalized startsAt, state, endsAt. Grafana TestAlert and ConciergeWebhookAcceptance are notification-only test conditions, no investigations.
4. Existing bot control HTTP listener gets a distinct alert path. Sandbox starts only alert handling on lane-fixed8380+lane with fresh run credentials, public sibling forwards there; production deployment listener remains disabled in sandbox.
5. Existing Concierge SQLite owns one current row per source/fingerprint, latest episode, monotonic state/revision, confirmed bot message timestamp and publication status. Transactional acceptance: duplicates no-op, older episodes ignored, resolved cannot return to firing in same episode. No raw payload storage.
6. Destination is the existing Thinkering agent channel C0C03E75160, passed as destinationChannel by the trusted runtime; sandbox substitutes only its provisioned core channel. Payload cannot choose destination. Investigation binds the channel registry's provider and code_path, preserving its native session mode. The root displays Grafana · FIRING or Grafana · RESOLVED, exact condition, fingerprint, startsAt and recovery time, plus omitted/truncated count if present. Existing scheduler serializes delivery: bot posts one root per fingerprint then updates that same root. Persist intent before Slack side effect. Ambiguous first post parks without blind retry; confirmed-root updates can retry bounded transient errors. Startup resumes pending work; dead ambiguous posts park after process identity proof. Receipts distinguish accepted/delivered/parked. Synthetic machine turn trigger keys use the existing native machine-turn identity mechanism; no fabricated Slack user timestamp or user claim.
7. First firing starts one read-only agent investigation through existing durable session queue with explicit machine_alert provenance and no slack_user_input_claims. At most one unfinished investigation per configured condition (seven); overlapping fingerprints coalesce into that investigation's current evidence. Repeated/recovery notifications never create another turn. One investigation per episode; terminal failure does not automatically retry. Native Stop remains available. Fixed prompt asks for bounded read-only current-state/runbook queries and findings; no polling, delegation, remediation or email authorization.
8. Normal bot-event rejection stays intact. Queued machine turns resolve provenance directly from alert state, not fabricated user claims. Human replies use ordinary routing. Machine alert roots must retain firing/recovery state when provider progress/final summaries occur; machine inputs do not become user-authored router search records.

## Cost and ownership
Only webhook/startup pending delivery triggers; O(instances per request), max64. Seven unfinished automatic investigations, no idle poller. Current rows grow with observed fingerprints; expected cardinality comes from the finite existing metric labels. No accumulating episode history or new retention process. Native Grafana grouping/repeat pacing governs arrivals.

## Verification and delivery
Focused auth/parser/ordering/dedup/crash tests and queued machine turn/progress tests. Exact-source four-lane Slack case via actual public sibling proves bot identity, firing/recovery same root, duplicate/auth rejection, zero captures and zero synthetic user claims, one real provider response. Required fresh Claude diff review and final Bun gate; one correction pass with affected sandbox reruns. Commit/push main and end provider turn; detached deployment owns activation, no rollout polling. Parent receives exact contract, private credential path and native firing/recovery procedure, with production readiness explicitly unverified until later acceptance.

## Review and handoff
Fresh Claude Code Opus5 session31e18dc2-3256-41cd-9eb1-4f05c62c7ab9 reviewed the whole plan; first Fable attempt returned quota429. Corrected destination/session binding, exact names, visible rendering and restart503 semantics. Source byte count is1503 (wc verified; review's manual1504 count was off by one), fully read. Later source1789451931.369269 confirms standard JSON/Bearer, UI-editable contact personal-observability-concierge, empty default receiver and activation only after endpoint readiness.

Parent and child provider turns must both end before native deployment can drain. Handoff is the pushed commit and sandbox receipt; deployment reactions report native rollout. No watcher/provider wake waits for deployment. A later user-initiated turn checks endpoint readiness and performs Grafana native TestAlert/recovery before routing the seven conditions. Sandbox URL is loopback-only and does not claim Grafana Cloud reachability. Private handoff is same-host root file reading; never publish token values. Queue-key rotation requires regenerating/verifying the derived bearer and updating Grafana's secure field together.
